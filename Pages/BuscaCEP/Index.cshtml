@page
@model MinhasFinancasWebApp.Pages.BuscaCEP.IndexModel
@{
	ViewData["Title"] = "Busca CEP";
}

<h1>Busca CEP</h1>

<a asp-page="/Menu" class="btn btn-secondary">Menu Principal</a>


<p>CEP:<input type="text"
			  id="cep"
			  name="cep"
			  placeholder="00000-000"
			  maxlength="9"
			  pattern="\d{5}-?\d{3}"
			  oninput="formatarCep(this)"
			  asp-for="cepConsultaText" />
</p>
<form method="post" asp-page-handler="Consulta" onsubmit="confirmarConsulta()">
<button class="btn-success" type="submit">Consultar CEP</button>
</form>

@section Scripts{
	<script>
		function formatarCep(input){
			let x =	input.value.replace(/\D/g,"");

			if (x.length > 8){
				x = x.substring(0,8);
			}

			if(x.length > 5){
				x = x.substring(0,5)+"-"+x.substring(5);
			}

			input.value = x;
		}

		function confirmarConsulta(input){
			return confirm("Deseja consultar o CEP?");
		}

		@if (TempData["MensagemErro"] != null){
			<text>
				alert('@TempData["MensagemErro"]');
			</text>
		}
	</script>
}